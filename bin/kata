#!/bin/bash
name=""

for i in $*; do
    if [ "$name" != "" ]; then
        name=$name"_"
    fi
    name=$name$i
done

name=${name//#}
name=$name".php"

if [ ! -f $name ]; then
    echo "<?php" > $name
    echo $name created.
else
    echo $name already exists!
fi
